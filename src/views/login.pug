extends base

block content
    div.loginPage
        div.loginHeight
            form(method="POST")
                if errorMessage
                    span=errorMessage
                div.nickname
                    input(placeholder="Nickname", name="nickname", type="text", required)
                div
                    input(placeholder="Password", name="password", type="password", required)
                div
                    input(type="submit", value="Login")
                div.auth-switch
                    span Don't have an account? 
                    a(href="/join") Create one now →